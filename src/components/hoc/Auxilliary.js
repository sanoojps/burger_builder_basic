/**
 * 
 * Component to wrap adjacent JSX components in  
 * a component 
 */

 /**
  * 
  * @param {props from the wrapping JSX} props 
  */
const aux = (props) => {
    return props.children 
}

export default aux